{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main-window access to core APIs and app-owned document commands; native file dialogs remain Rust-owned.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
